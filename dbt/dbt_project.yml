name: 'spotify_music_analytics'
version: '1.0.0'
config-version: 2

profile: 'spotify_music_analytics'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  spotify_music_analytics:
    staging:
      +materialized: view
    mart:
      +materialized: table
